# VolleyForm AI Coach — 系統功能說明

> 本文件描述 **2026-09-21 實際上線的系統**(build `20260920-frontend-sync-v109`,commit `abefbc0`)。
> 每一項功能都對得到程式碼位置,每一個數字都是在寫這份文件時重新量過的。
> 錯誤清單與各項檢查的證據見 [`ERROR_TAXONOMY.md`](ERROR_TAXONOMY.md);
> 資料集每一支片的來源與拒收理由見 [`dataset/MANIFEST.md`](dataset/MANIFEST.md)。

---

## 一、系統定位

使用者用手機拍自己打排球,App 指出**動作哪裡不符合參考資料集建立的標準**,
並給出對應的修正建議。

**它不是一個訓練出來的模型。** 沒有 loss function、沒有梯度、沒有權重檔,
`torch` / `tensorflow` / `sklearn` 一個都沒安裝。它是**一套可稽核的統計基準線**:
每一條門檻都追得到是哪幾支影片、哪個百分位算出來的。

支援五個動作:**扣球 spike、發球 serve、攔網 block、接球 receive、舉球 set**。

---

## 二、執行架構

| | 說明 |
|---|---|
| **主要形態** | 純前端 PWA,部署在 GitHub Pages,**不需要後端** |
| **執行位置** | 影片不上傳,全部在使用者瀏覽器裡跑完 |
| **後端** | 可選。`backend/server.py` 提供同樣的分析 API,供本機或雲端部署使用 |
| **離線** | Service Worker 快取(`volleyform-shell-v109-…`),模型檔一併快取後可離線分析 |

前端與後端**各自實作一份評估器**(`frontend/local-analyzer.js` 94.3 KB 與
`backend/reference_evaluation.py` 18.3 KB),由 `frontend/evaluation_parity_test.mjs`
釘住兩者一致,包含 16 組跨語言數值對照。

---

## 三、處理流程

```
影片
 └─ 取樣      OpenCV / 瀏覽器 seek，縮到 640 寬（只縮不放）
     └─ 姿勢   MediaPipe → 33 個身體 landmark（+ 21×2 手部 landmark）
         └─ 角度   手肘／肩膀／膝蓋，以及 6 個形狀特徵
             └─ 階段切割   找出「觸球」與「蓄力」是哪一格
                 └─ 判定   對照參考帶 → 錯誤代碼
                     └─ 回饋   文字建議 ＋ 3D 小人 ＋ 錯誤影格骨架
```

### 3.1 取樣

**兩條路徑,取格方式不同**,2026-09-20 才把**寬度**對齊:

| | 校準與後端 API | 前端本機分析(使用者實際跑的) |
|---|---|---|
| 程式 | `pose/pose.py` | `frontend/local-analyzer.js` |
| 取哪些格 | 每 N 格取一格(`frame_stride`,校準用 2) | 在整支片上**等距 seek** `sampleCount` 次 |
| 取幾格 | 由 stride 與片長決定 | `max(模式下限, 秒數×2.5 進位)`,再壓到模式上限 |
| 寬度 | `process_width`(函式預設 720,`build_reference.py` 傳 **640**) | 固定 `PROCESS_WIDTH = 640` |

前端三個模式的取樣格數:**省電 16–32、平衡 26–54、高品質 40–80**。
(`app.js` 另有一組 `frame_stride` / `process_width` / `max_frames` 預設值——
那是**上傳到後端 API** 時才送出的參數,不影響本機分析。)

**縮放只縮不放**:原本就窄於 640 的影片原樣送進模型,不會被放大成相機沒拍到的細節。

> 640 這個數字不是隨便選的——**建立判定帶時用的就是 640**,而 120 支參考片裡有 61 支比它寬。
> 2026-09-20 之前前端是用原生解析度,量出來的後果不在角度(中位數只差 1.4 度,
> 因為 landmark 本來就正規化過),而在**階段切割器挑到不同影格**:十支高解析片裡
> 16 個階段有 8 個選到不同格,最嚴重一支差了 25 格。判到不同的瞬間不是捨入誤差。

### 3.2 姿勢估計

**這一層用的是 Google 訓練好的模型,我們沒有微調。**

| | 模型 | 設定 |
|---|---|---|
| 前端(使用者實際跑的) | `pose_landmarker_full.task`(9.4 MB)<br>`hand_landmarker.task`(7.8 MB) | MediaPipe Tasks、CPU、VIDEO 模式、單人、雙手 |
| 前端省電模式 | `pose_landmarker_lite.task`(5.8 MB) | 同上 |
| 後端 | MediaPipe **Holistic** | `model_complexity=1`,偵測/追蹤信心 0.45 |

### 3.3 角度與特徵

`angle/angle.py`(13.6 KB)。

**三個關節角度**(判定帶用的):

| 角度 | 三個點 | 左右怎麼合併 |
|---|---|---|
| 手肘 | 肩 → 肘 → 腕 | 後端 `max(左,右)`;**前端取平均** |
| 肩膀 | 肘 → 肩 → 髖 | 後端 `max(左,右)`;**前端取平均** |
| 膝蓋 | 髖 → 膝 → 踝 | 後端 `min(左,右)`;**前端取平均** |

> 前後端的合併方式**不同**,這是已知且被測試釘住的分歧,尚未統一(見第八節)。

**六個形狀特徵**(角度表達不了的關節間關係),全部在影像平面上量、除以**軀幹長度**:

| 特徵 | 量什麼 |
|---|---|
| `hand_level_gap` | 兩手的高度差 |
| `hand_gap` | 兩手的水平距離 |
| `stance_width` | 兩腳踝的水平距離 |
| `wrist_above_shoulder` | 較高的手高過肩多少 |
| `hands_off_center` | 雙手中點離鼻子多遠 |
| `trunk_lean` | 肩中點在髖中點前面多少 |

另有 `shoulder_hip_width_ratio`(肩寬/髖寬)供側身判斷使用。

> 分母用**軀幹長度**而非肩寬,是量出來的決定:肩寬是投影量,球員側身時兩肩投影到
> 同一點、分母塌向 0。舊版有一支發球片的 `hand_level_gap` 量到 **81.5**——
> 一個高度只有 1 個單位的畫面,兩手不可能相距 81 個身寬。

### 3.4 階段切割

`backend/phase_segmentation.py`(4.1 KB)。**全部是算術,沒有模型。** 所有序列先做 3 格平滑。

| 動作 | 觸球 contact | 蓄力 crouch |
|---|---|---|
| 扣球／發球／攔網 | 平滑後**手腕 y 的全域最小值** | 觸球之前**髖部 y 最大**的一格 |
| 舉球 | 每格算分取最小:雙手均高與(鼻高+0.03)的差 ＋ 兩手水平距×1.2 ＋ 高度差 ＋ 手沒過肩罰 0.35 | 觸球 **±4 格**內髖部最低 |
| 接球 | 同上,目標高度改 `肩高+0.18`,水平距×1.8、高度差×1.2、手沒低於肩罰 0.35 | 觸球 **±4 格**內髖部最低 |

**三種棄權路徑**(設計上有,但不夠):

- 影格少於 6 格 → 整個不做階段切割,退回 legacy 模式
- 扣球／發球／攔網:如果觸球前的髖部完全沒下沉(< 0.02)→ **crouch = None**,不判蓄力
- 舉球／接球的 crouch 用固定 ±4 格視窗,**不會棄權**

**沒有棄權的是 contact**——只要影格夠,它一定回傳某一格,即使那一格根本不是觸球。
這是已知缺陷,見第八節。

---

## 四、判定機制

### 4.1 判定帶怎麼來的

`tools/build_reference.py` → `backend/reference_standards.json`(產出日 2026-09-17)。

```
120 支參考片（五個動作資料夾）
  → 每支切出觸球格與蓄力格
  → 丟掉「畫面外硬猜」的關節（見 4.4）
  → 1.5×IQR 圍籬剔除離群值
  → 取 p10 / p90
  → 加上自適應寬容度
  → accepted_range = [p10 − tolerance, p90 + tolerance]
```

**自適應寬容度**:

```
tolerance = clamp(
      下限
    + min(6.0, IQR × 0.18)                    # 分布越散 → 越寬
    + min(5.0, 9 / sqrt(n))                   # 樣本越多 → 越窄
    + player_grace × (1 − min(0.45, n/60)),   # 樣本越多 → 越窄，但最多只收斂 45%
    下限, 上限)
```

| 關節 | 下限 | 上限 | player_grace |
|---|---|---|---|
| 手肘 | 8.0 | 22.0 | 4.0 |
| 肩膀 | 8.0 | 22.0 | 5.0 |
| 膝蓋 | 10.0 | 24.0 | 6.0 |

`player_grace` 存在的理由是:業餘球員的正確動作不該只拿菁英的活動度來量。
判定端另有 `MAX_BAND_TOLERANCE = 24.0` 再夾一次。

**寬容度的方向**:`低 = p10 − tolerance`、`高 = p90 + tolerance`。
所以**寬容度越大,帶越寬,被標紅的片越少**;越小則帶越窄、標紅越多。

**高側另有直肢上限 `STRAIGHT_LIMB_FLOOR = 168°`**:當這條帶有高側規則、
且**最大保留樣本**低於 168 時,上界卡在 168。否則對稱的寬容度會把上界推到 180,
那裡永遠不可能觸發,檢查等於是死的。

### 4.2 目前上線的判定

**角度判定帶 16 條**(`ACTION_RULES`):

| 動作 | 階段 | 關節 | 低側代碼 | 高側代碼 |
|---|---|---|---|---|
| 扣球 | 觸球 | 手肘 | `elbow_bad` | — |
| 扣球 | 觸球 | 肩膀 | `shoulder_low` | — |
| 扣球 | 蓄力 | 膝蓋 | `knee_too_bent` | `knee_bad` |
| 發球 | 觸球 | 手肘 | `elbow_bad` | — |
| 發球 | 觸球 | 肩膀 | `shoulder_low` | — |
| 發球 | 蓄力 | 膝蓋 | `knee_too_bent` | `knee_bad` |
| 攔網 | 觸球 | 手肘 | `elbow_not_straight` | — |
| 攔網 | 觸球 | 肩膀 | `hands_not_high` | — |
| 攔網 | 蓄力 | 膝蓋 | `knee_too_bent` | `knee_bad` |
| **攔網** | **蓄力** | **手肘** | **`block_arms_dropped`** | — |
| 接球 | 觸球 | 手肘 | `elbow_bad` | — |
| 接球 | 觸球 | 膝蓋 | `knee_too_bent` | `knee_bad` |
| 接球 | 觸球 | 肩膀 | *(規則刻意留空)* | *(留空)* |
| **舉球** | **蓄力** | **膝蓋** | — | **`set_legs_not_used`** |
| 舉球 | 觸球 | 手肘 | `elbow_position_bad` | `elbow_position_bad` |
| 舉球 | 觸球 | 肩膀 | `shoulder_low` | — |

> **接球觸球肩**的帶有算出來(n=22、收斂度 0.78),前端也顯示,
> 但規則是空的,所以它**只顯示不判定**——那個位置改由 `lobster_receive_risk`
> 同時看肩與肘來處理。

**形狀檢查 1 條**:

| 量什麼 | 代碼 | 接受範圍 |
|---|---|---|
| `set.contact.hands_off_center`(觸球點偏離頭部中線) | `set_hands_off_forehead` | 上界 **0.429** 軀幹長(無下界) |

> 21 個樣本從 0.004 排到 0.327,零剔除,上界 0.429 在所有樣本之外——
> 校準當作正確的片,不可能踩到它。

**非帶狀檢查 8 條**(`_evaluate_hand_shape`,門檻寫死,**沒有經過百分位校準**):

| 代碼 | 動作 | 條件 |
|---|---|---|
| `receive_platform_unbalanced` | 接球 | 兩手高度差 > 0.08 |
| `receive_hands_apart` | 接球 | 兩手中心距 > 0.24 |
| `lobster_receive_risk` | 接球 | 手肘 < 125° **且** 肩 < 55° |
| `wrist_low` | 舉球 | 手腕低於頭頂 |
| `setting_hands_not_detected` | 舉球 | 偵測到的手 < 2 |
| `setting_fingers_closed` | 舉球 | 手指伸展度 < 1.08 |
| `setting_hand_spacing_bad` | 舉球 | 兩手中心距 < 0.06 或 > 0.32 |
| `setting_hands_unbalanced` | 舉球 | 兩手高度差 > 0.08 |

> 唯一有「半個校準來源」的是 `lobster_receive_risk` 的手肘門檻:它取
> `min(125.0, 接球手肘接受下界 − 4.0)`,所以判定帶動它就跟著動。
> 目前接球手肘下界 130.8,減 4 是 126.8,**實際生效的仍是寫死的 125.0**。

**錯誤代碼共 18 個**,`ERROR_FEEDBACK` 另有 `good` / `unknown_action` 兩個狀態列,共 20 列。
每個錯誤代碼在**五張表**都必須有文字,由 `frontend_sync_test.py` 守住:

| 表 | 位置 | 內容 |
|---|---|---|
| `ERROR_FEEDBACK` | `backend/feedback.py` | 標題、嚴重度、訊息、修正法 |
| `ISSUE_DETAILS` | `backend/feedback.py` | 部位、即時口令、練習方法、為什麼重要 |
| `ISSUE_JOINT_STATUS` | `frontend/local-analyzer.js` | 3D 小人哪個關節上什麼顏色 |
| `phaseIssueTips` | `frontend/app.js` | 階段建議 |
| `phaseProblemPhrases` | `frontend/app.js` | 問題描述 |

> 這張檢查表是被 bug 逼出來的:`lobster_receive_risk` 曾經好幾個月沒有
> `phaseProblemPhrases` 條目,本機分析的使用者只看得到最含糊的那句預設文字。

### 4.3 判定帶現況(2026-09-17 產出)

| 動作 | 片數 | 階段.關節 | n | 收斂度 | 狀態 | p10 / p50 / p90 | 接受範圍 |
|---|---|---|---|---|---|---|---|
| 扣球 | 20 | contact.elbow | 18 | 0.89 | stable | 141.0 / 156.9 / 167.0 | 125.7 – 180.0 |
| | | contact.shoulder | 19 | 0.79 | stable | 111.2 / 148.3 / 163.0 | 91.7 – 180.0 |
| | | crouch.knee | 10 | 0.67 | **usable** | 115.5 / 139.1 / 148.7 | 94.8 – 168.0 |
| 發球 | 26 | contact.elbow | 23 | 0.87 | stable | 141.5 / 162.9 / 168.7 | 126.2 – 180.0 |
| | | contact.shoulder | 23 | 0.84 | stable | 122.3 / 133.9 / 155.7 | 105.2 – 172.8 |
| | | crouch.knee | 14 | 0.79 | stable | 138.4 / 152.8 / 167.7 | 118.9 – 180.0 |
| 攔網 | 26 | contact.elbow | 21 | 0.89 | stable | 141.9 / 155.1 / 168.1 | 126.9 – 180.0 |
| | | contact.shoulder | 22 | 0.84 | stable | 124.2 / 139.2 / 166.9 | 107.0 – 180.0 |
| | | crouch.knee | 12 | 0.60 | **usable** | 82.4 / 118.8 / 148.8 | 59.0 – 168.0 |
| | | crouch.elbow | 22 | 0.78 | stable | 92.6 / 140.3 / 163.1 | 74.1 – 180.0 |
| 接球 | 24 | contact.elbow | 21 | 0.89 | stable | 145.7 / 162.3 / 171.5 | 130.8 – 180.0 |
| | | contact.knee | 19 | 0.76 | stable | 89.0 / 121.4 / 149.7 | 66.8 – 168.0 |
| | | contact.shoulder | 22 | 0.78 | stable | 24.1 / 59.3 / 100.8 | 5.0 – 119.9 *(不判定)* |
| 舉球 | 24 | contact.elbow | 22 | 0.85 | stable | 103.5 / 120.5 / 150.6 | 87.2 – 166.9 |
| | | contact.shoulder | 21 | 0.79 | stable | 83.5 / 102.2 / 132.5 | 64.3 – 151.7 |
| | | crouch.knee | 12 | 0.64 | **usable** | 111.0 / 133.0 / 150.2 | 88.0 – 168.0 |

**三條 `usable` 全部是蓄力膝蓋**——近距離教學片常常切掉腿,所以這三條薄。

**收斂度**不是準確率,只是「樣本夠不夠、集中不集中」:

```
收斂度 = 0.45 × min(1, 原始樣本數/20)
       + 0.45 × 1/(1 + IQR/45)
       + 0.10 × (1 − min(0.5, 離群數/原始樣本數))

stable : 原始樣本 ≥ 14 且分數 ≥ 0.65      usable : ≥ 5 且 ≥ 0.45
```

中間那一項讓分數實務上上不去:IQR 二十幾度時它只值 0.32 左右,
所以 16 條帶的最高分是 **0.89**。**收斂度高不等於標準正確**——
同一個示範者拍的一批片會收斂得很漂亮,同時把帶收窄到開始誤判正確動作。

### 4.4 兩道「不該判就別判」的 gate

這兩道的共同點是:**擋掉不可信的輸入,而不是調整門檻。**

**畫面外 gate**(`angle.angle.landmarks_offscreen`,`OFFSCREEN_MARGIN = 0.10`)

兩層規則,兩層都是量出來的:

- 明顯在畫面外(超過邊界 0.10 以上)→ **不管信心多高都丟**
- 剛好切到邊(0 到 0.10)→ 只在信心 < 0.5 時丟

> MediaPipe 不會說「我看不到腿」,它會**有信心地**給一個畫面外的位置。
> 校準端從 2026-08-17 就丟掉這些樣本,但**判定端一直照單全收**——
> 校準說不能用的資料,在給使用者看的那一刻被當成真的。
> 量出來:被帶標紅的 25 個樣本中,**4 個算在硬猜的關節上**
> (含解剖學上不可能的 `block crouch knee 23.6°`,以及那支 `angle.py`
> 註解裡就記著同一個失敗的 177.8° 舉球手肘)。
> 2026-09-17 補上後,被誤判的片從 20 支降到 16 支(**16.7% → 13.3%**)。

**手部偵測可信度 gate**(`HAND_PAIR_MIN_SEPARATION = 0.10`)

兩手間距小於 0.1 個手掌寬 → 那不是兩隻手,是同一隻被偵測兩次。

> `setting_hand_spacing_bad` 曾在能執行的 6 支參考片裡打中 4 支(67%)。
> 門檻沒錯,輸入錯了:量到的間距是 0.0006,而一個手掌就有 0.07 寬。
> 加 gate 後誤傷歸零、真實判定零損失。`finger_extension` 刻意留在 gate 外——它量單手。

---

## 五、使用者看到什麼

| 功能 | 說明 | 檔案 |
|---|---|---|
| **影片分析** | 上傳或現拍,等距取樣後給出問題清單與時間點 | `local-analyzer.js` |
| **照片分析** | 單張照片的即時判定(無階段切割) | 同上 |
| **即時分析** | 開鏡頭邊打邊看 | 同上 |
| **3D 姿勢比對** | 左=你的姿勢(紅/黃/綠標色)、右=正確示範動畫 | `pose-3d.js`(108.8 KB) |
| **錯誤影格骨架** | 2D canvas 畫出出問題那一格的骨架 | `app.js`(61.1 KB) |
| **教練式回饋** | 每個錯誤給:問題描述、即時口令、練習方法、為什麼重要 | `feedback.py` |
| **QR 分享** | 產生連結讓別人開同一份結果 | `app.js` |
| **多模態欄位** | 已啟用:身體骨架、手部關節。保留欄位:球路、聲音、穿戴感測、教練備註 | `modalities.py` |
| **省電/平衡/高品質** | 影響取樣格數(16–32 / 26–54 / 40–80)與模型大小 | `app.js` |

**3D 示範動畫**(`pose-3d.js`)本身受測試約束:`pose_geometry_test.mjs` 會拿小人的姿勢去對
App 自己的判定帶,**示範動作必須通過自己的標準**。這個測試已經抓到示範與教學文字互相矛盾
(接球示範曾經彎肘 120°,而 App 自己的口令是「手臂要伸直」)。

---

## 六、品質保證

### 6.1 測試(16 項:13 個 Python + 3 個 Node)

最後一次全跑(2026-09-21):

| 環境 | 結果 |
|---|---|
| 專案指定的 `.venv`(Python 3.12.13 + MediaPipe 0.10.21 + OpenCV 4.11.0) | **15 通過、1 不穩定** |
| 系統 Python(3.10.9 + MediaPipe 0.10.9 + OpenCV 4.13.0) | 16 通過、0 失敗 |

> **指定環境下 `self_test.py` 會間歇性崩潰**:連續跑 10 次有 **4 次**以
> heap corruption(`0xC0000374`)結束,而同一支測試在系統 Python 上跑 10 次全過。
> 崩在 HTTP 服務與 MediaPipe 原生庫同行程並存的地方,影響範圍是可選的後端 API
> 與離線維運流程,**線上裝置端版本不受影響**。
> 上一版本文寫的「16 通過、0 失敗」是在**系統 Python** 上跑出來的,不是指定環境。

| 測試 | 守什麼 |
|---|---|
| `phase_reference_test.py`(34.6 KB) | **correctness 的本體**——各動作的階段選擇、每條新帶的行為、兩道 gate |
| `angle_acceptance_test.py` | 每條規則「輕微偏差要放行、明顯錯誤要觸發」 |
| `clip_quality_test.py` | 掃描器的幀品質 gate(畫面外、邊緣容忍、遮擋、軀幹下限、被追蹤者連續性) |
| `shape_check_test.py` | 形狀檢查的行為 + 跨語言 fixture |
| `evaluation_parity_test.mjs` | **前後端一致性**——門檻、landmark 表、兩道 gate、處理寬度、16 組數值對照 |
| `pose_geometry_test.mjs` | 3D 示範動畫的幾何,以及它是否通過 App 自己的標準 |
| `frontend_sync_test.py` | 每個錯誤代碼在五張表都有文字 |
| `feedback_contract_test.py` | 回饋表的結構完整 |
| `reference_convergence_test.py` | 判定帶的收斂度與樣本數沒有悄悄退步 |
| `frontend_contract_test.py` / `frontend_quality_test.py` | 前端契約與品質 |
| `resource_contract_test.py` | 模型檔、快取清單等資源存在且被登記 |
| `backend_no_ui_test.py` | 後端不依賴任何 UI |
| `user_expectation_test.py` / `self_test.py` | 端到端與自我檢查 |

**每個新加的守門員都要做變異測試**(把它該抓的 bug 注回去,確認真的會失敗)。
這個做法已經抓到多個瞄錯對象的斷言。
跑變異測試務必加 `PYTHONDONTWRITEBYTECODE=1`,否則陳舊的 `.pyc` 會給出假的通過。

### 6.2 混淆矩陣(以影片為單位,2026-09-21 重跑)

| | App 說有問題 | App 說沒問題 |
|---|---|---|
| **實際正確**(120 支參考片) | **16** 誤判 | 104 放行 |
| **實際錯誤**(3 段標記片段) | 1 抓到 | 2 漏掉 |

**誤判率 16/120 = 13.3%**(加畫面外 gate 前是 16.7%)。
按動作分:發球 7 支、扣球 4 支、接球 3 支、攔網 1 支、舉球 1 支。

三件要講清楚的:

1. 右欄母體只有 **3**,那個「33% 抓到率」不是準確率,只供參考
2. 左欄的「實際正確」是**定義**不是獨立判斷——標準就是從它們算出來的,所以那一欄可信
3. **誤判率壓不到 0 是算術**:門檻是 `p10 − tolerance`,而 p10 依定義把 10% 的樣本放在下面。
   16 條帶裡有 3 條的低側門檻**高於它自己最小的保留樣本**,那是設計,不是 bug

### 6.3 新增檢查的驗收門檻

| | 條件 | 適用 |
|---|---|---|
| **標準級** | (a) 清掉每一支參考片 + (c) 檢查不是死的 | **新加的**角度判定帶 |
| **嚴格級** | (a) + (c) + (b) 抓得到獨立的、標記為錯誤的片段 | 全新的特徵型態 |

**標準級只支持一句話**:「量到的角度／特徵,落在由假定正確的參考片建立的接受範圍之外」。
它**不支持**「所以你犯了 X 這個錯誤」。

目前 16 條帶裡**只有 `block_arms_dropped` 有標記錯誤的素材佐證**。

> 為什麼不去抓公開資料集:排球的錯誤標記資料集**在結構上不存在**——
> AQA 資料集只出現在有官方評分(體操、跳水)或臨床流程的領域,
> 而排球計分計的是**回合**,不是動作。詳見 `ERROR_TAXONOMY.md` 開頭與
> Obsidian 的 `30-decisions/2026-09-20-volleyball-error-labels-do-not-exist`。

---

## 七、資料集

| 資料夾 | 片數 |
|---|---|
| `dataset/spike` | 20 |
| `dataset/serve` | 26 |
| `dataset/block` | 26 |
| `dataset/receive` | 24 |
| `dataset/set` | 24 |
| **合計參考片** | **120** |
| `dataset/_candidates`(未分類) | 45 |
| `dataset/_rejected`(隔離) | 1 |

**影片本身不進版控**(大小與授權),只 commit 推導出來的統計。
`dataset/MANIFEST.md` 是唯一記錄「為什麼資料集長這樣」的地方,**包含每一次拒收的理由**。
授權來源限**自由授權(Pexels)與使用者自備檔案**;**不從 YouTube 下載**。

收片流程(`.claude/skills/volleyform-dataset-clips`):

```
1. probe     解析度、長度，以及影片實際拍了什麼
2. scan      MediaPipe 掃出候選窗口
3. montage   標註過的聯絡表   ← 人在這裡看
4. trim      只切確認過的窗口
5. rebuild   tools/build_reference.py
6. gate      全套 16 項測試；correctness 退步就還原
7. record    寫進 MANIFEST.md，包含拒收
```

**步驟 3 和 6 是真正會抓到東西的。** 最常見的正確結果是**拒收素材**。

掃描器共用一份幀品質 gate(`tools/dataset_clips/clip_quality.py`),
用的就是判定端那一道畫面外規則,外加兩項掃描器專用的:

| 檢查 | 門檻 | 為什麼 |
|---|---|---|
| 軀幹下限 | `TORSO_FLOOR = 0.06` | 一場 16 分鐘的比賽片量到軀幹只有畫面高的 **0.008**——幾十個像素,landmark 雜訊和要量的角度一樣大。0.06 在 209 個參考樣本裡只排除 1 個,0.10 會排除 12 個 |
| 被追蹤者連續性 | `SUBJECT_JUMP = 0.15` | MediaPipe 永遠回傳一個人,從不說「這裡沒人」。場上十二個人時它就挑一個,那支比賽片**換了 167 次**(約每 5.8 秒一次)。曾經有一支從頭到尾追錯人的影片通過了當時所有檢查 |

---

## 八、已知限制

誠實列出,不要在報告裡迴避:

| 限制 | 現況 |
|---|---|
| **8 個手部門檻沒有校準來源** | 寫死的常數,沒有樣本分布可稽核(只有 `lobster_receive_risk` 的手肘半掛在帶上,而且目前沒生效) |
| **階段切割器會挑錯影格** | leave-one-out 標紅的 20 支片裡,**7 支是量在明顯不是觸球的影格上**(手腕根本沒過鼻/過肩)。contact 沒有棄權路徑,只要影格夠就一定回傳一格 |
| **前後端左右合併方式不同** | 後端取 `min`/`max`,前端取平均;測試釘住現狀但未統一 |
| **量測誤差未知** | 寬容度 8–24 度是在**不知道 MediaPipe 本身誤差多大**的情況下訂的(SportsPose 使用申請已寄出,等回覆) |
| **三條帶只有 10–12 個樣本** | 全部是蓄力膝蓋,狀態 `usable` 而非 `stable` |
| **8 項錯誤做不到** | 需要球、網或時序資訊,例如過網時機、拋球位置、助跑步伐 |
| **標準級檢查的文字寫成了診斷** | `set_legs_not_used` 與 `set_hands_off_forehead` 的措辭斷言了原因,違反驗收門檻自己的規定,待修 |
| **3D 小人對「沒判」的關節顯示綠色** | 不會誤導成錯誤,但也沒提示使用者重拍 |
| **收斂度上不去** | 最高 0.89,而且它衡量的是集中度不是正確性 |
| **指定環境下後端煙霧測試會崩** | `self_test.py` 在 `.venv` 上 10 次有 4 次 heap corruption(見 6.1) |
| **模型檔沒有預先快取** | `APP_SHELL` 只快取 18 項外殼資源,三個 `.task`(約 23 MB)不在其中,靠執行期快取。裝了 PWA 但沒在連線時分析過一次的人,離線只能開介面、不能分析 |

---

## 九、技術棧

| | |
|---|---|
| 姿勢估計 | MediaPipe 0.10.21(Tasks 前端 / Holistic 後端) |
| 影像處理 | OpenCV 4.11 |
| 3D 呈現 | three.js r160(vendored) |
| 前端 | 原生 ES module,無框架 |
| 後端(可選) | Python 標準函式庫 HTTP server |
| 部署 | GitHub Pages + GitHub Actions |
| **機器學習框架** | **無**——torch / tensorflow / sklearn 皆未安裝,repo 內無任何權重檔 |

### 主要模組

| 檔案 | 大小 | 職責 |
|---|---|---|
| `pose/pose.py` | 3.8 KB | 影片取格、縮放、MediaPipe 推論 |
| `angle/angle.py` | 13.6 KB | 角度、形狀特徵、畫面外 gate(**前後端共用的定義來源**) |
| `backend/phase_segmentation.py` | 4.1 KB | 階段切割 |
| `backend/reference_evaluation.py` | 18.3 KB | 判定帶、形狀檢查、手部檢查、兩道 gate |
| `backend/feedback.py` | 15.6 KB | 錯誤代碼 → 文字(兩張表) |
| `backend/analyzer.py` | 14.4 KB | 後端分析流程串接 |
| `backend/action_registry.py` | 1.5 KB | 五個動作的登記 |
| `backend/modalities.py` / `modality_processors.py` | 2.3 / 3.4 KB | 多模態欄位 |
| `backend/server.py` | 6.6 KB | HTTP API |
| `frontend/local-analyzer.js` | 94.3 KB | **前端完整重實作**:取樣、推論、切割、判定、回饋 |
| `frontend/app.js` | 61.1 KB | UI、模式設定、2D 骨架、QR 分享 |
| `frontend/pose-3d.js` | 108.8 KB | 3D 小人與示範動畫 |
| `tools/build_reference.py` | — | 從資料集產生判定帶 |
| `tools/dataset_clips/clip_quality.py` | — | 掃描器共用的幀品質 gate |
