# 姿態估計模型比較：VolleyForm 為什麼用 MediaPipe

> 狀態：**部分實測**。MediaPipe 的數字是在本機跑出來的；RTMPose / YOLO-Pose / ViTPose
> 的欄位是文獻與官方數據，尚未在本專案的影片上驗證過。哪些是實測、哪些是引用，
> 表格中一律標明。未標「實測」的數字不得寫進結論。

## 1. 這份文件要回答的問題

不是「哪個模型最準」，而是「**對 VolleyForm 這個系統**，換掉 MediaPipe 划不划算」。
這兩個問題的答案不一樣，因為 VolleyForm 的限制條件很特殊：

- 判定完全建立在**關節角度**上，不是影像特徵
- 前端要能**在瀏覽器即時跑**，沒有 GPU 後端預算
- 使用者是自己拍的手機影片，不是標準拍攝環境

## 2. 實測設定

| 項目 | 設定 |
|---|---|
| 測試影片 | `dataset/` 內 5 個動作 × 2 支 = 10 支真實排球片 |
| 影格 | 每支取**中段連續 30 格**（非跳格），共 300 格 |
| 解析度 | 全部統一縮到寬 960px 再餵給模型 |
| 硬體 | Intel 13/14 代行動處理器，16 執行緒，**純 CPU，無 GPU** |
| 計時範圍 | 只算推論，不含 PNG 解碼 |

兩個刻意的設計決定：

- **連續影格而非跳格。** MediaPipe 的 video mode 靠前後格追蹤加速，跳格會破壞它的
  追蹤狀態，等於在一個 VolleyForm 從不使用的模式下測它。
- **影格預先抽成 PNG。** 各 backend 的環境裝不同版本的 OpenCV（專案 4.13、
  測試環境 5.0），若各自解碼，編解碼差異會偽裝成模型差異。

程式碼在 [`tools/pose_benchmark/`](tools/pose_benchmark/)，可重跑。

## 3. MediaPipe 實測結果

| 動作 / 影片 | 抓到姿態 | 中位延遲 | FPS | 關節點信心 ≥0.30 比例 |
|---|---|---|---|---|
| spike / 6179970 | 30/30 | 30.8 ms | 32.5 | 73.1% |
| spike / 6216849 | 30/30 | 30.7 ms | 32.5 | 87.9% |
| serve / 10350521 | 30/30 | 32.1 ms | 31.2 | 88.5% |
| serve / 10350524 | 30/30 | 30.7 ms | 32.6 | 80.3% |
| receive / 12169404 | 30/30 | 30.7 ms | 32.6 | 100.0% |
| receive / 12169435 | 30/30 | 31.5 ms | 31.8 | 82.8% |
| set / 6179974 | 30/30 | 31.2 ms | 32.0 | **55.9%** |
| set / 6217115 | 30/30 | 31.7 ms | 31.5 | 69.2% |
| block / 10350518 | 30/30 | 31.6 ms | 31.6 | 88.7% |
| block / 10350520 | 30/30 | 34.1 ms | 29.3 | 84.6% |
| **全部** | **300/300** | **31.3 ms** | **≈32** | — |

### 兩個必須一起讀的數字

**300/300 抓到姿態，但只有 69.0% 的關節角度算得出來**（1656 / 2400）。

這不是矛盾，是 MediaPipe 的一個性格：**它幾乎不會說「我找不到人」**，一定回傳
33 個點，找不到的部分用信心值標低。所以：

- 「偵測成功率 100%」這個數字**單獨看是誤導的**，不該寫進報告當賣點
- 真正該報的是角度可算率 69%
- `set` 動作最慘（55.9%）——舉球時手在臉前、身體正面朝鏡頭，手肘與髖部互相遮擋

這一點直接連到專案既有的踩雷紀錄：三項形狀檢查曾放行一支從頭到尾追錯人的影片，
以及 `setting_hand_spacing_bad` 曾在 6 支參考片誤傷 4 支（兩手偵測疊在同一隻手上）。
**根因同一個：MediaPipe 回傳的點永遠「看起來」是完整的。**

## 4. 模型比較表

實測欄只有 MediaPipe 有值，其餘為引用。

| | **MediaPipe (BlazePose)** | **RTMPose-m** | **YOLO11-Pose** | **ViTPose / ViTPose++** | **Sapiens** |
|---|---|---|---|---|---|
| 關節點 | **33**（含臉、腳掌） | 17 (COCO) | 17 (COCO) | 17 (COCO) | 308 |
| 3D 深度 | **有**（world landmarks） | 無 | 無 | 無 | 有(需另配) |
| COCO AP | ~65 (引用) | 75.8 (引用) | ~70 (引用) | >80 (引用) | SOTA (引用) |
| 實測 OKS | — | .746 (引用) | — | .717 (引用) | .715 (引用) |
| CPU 速度 | **32 FPS（實測）** | 70–90 FPS (引用, i7) | ~10 FPS (引用) | 需 GPU | 需 GPU |
| **能在瀏覽器跑** | **✅ 官方 JS/WASM** | ❌ | ❌ | ❌ | ❌ |
| 需要後端 GPU | ❌ | 建議 | 建議 | **✅ 必須** | **✅ 必須** |
| 多人 | 單人（自動選主體） | 多人 | 多人 | 多人 | 多人 |
| 內建人物偵測 | **✅ 一體** | 需配 RTMDet | ✅ 一體 | 需另配 | 需另配 |
| 手部 21 點 | **✅ Holistic** | 需 Wholebody | ❌ | ❌ | ✅ |
| 安裝體積 | ~50 MB | ~50 MB + ORT | **~2.5 GB**(torch) | ~2.5 GB + 模型 | 數 GB |
| 授權 | Apache 2.0 | Apache 2.0 | **AGPL-3.0** ⚠️ | Apache 2.0 | 需確認 |

## 5. MediaPipe 對本專案的四個不可替代優勢

按重要性排，不是按技術先進度排。

### 5.1 唯一能在純前端跑的（決定性）

這不是「比較方便」，是**架構層級的差異**。VolleyForm 部署在 GitHub Pages，
零伺服器成本。換成 RTMPose 或任何 COCO 系模型，就必須：

租 GPU 後端 → 上傳使用者影片 → 處理隱私與流量成本 → 從免費變月費

其他所有模型的精度優勢，都必須先付這筆錢才拿得到。

### 5.2 有 world 3D 座標，其餘 2D 模型沒有

專案的 `angle/angle.py` 用的是 `calculate_angle_3d()`，跑在
`pose_world_landmarks` 上。RTMPose / YOLO-Pose / ViTPose **只輸出 2D**。

換模型不是換一行 import，而是：

1. 全部 14 條判定帶的門檻要重新校準（2D 角度 ≠ 3D 角度）
2. 或者另外接一個 2D→3D lifter（MotionBERT / MixSTE），多一層模型與誤差

`reference_standards.json` 的百分位帶是用 3D 角度算出來的，換 2D 等於整套作廢。

### 5.3 33 點含臉部與腳掌，判定直接依賴

- 「contact 格手腕須高於鼻子」用到 **nose** — COCO 有
- 手部 21 點間距檢查（`HAND_PAIR_MIN_SEPARATION`）用到 **Holistic 的手部模型** — COCO 沒有
- 腳掌朝向 — COCO 只到腳踝

改用 COCO-17 會直接砍掉現有的形狀檢查。

### 5.4 授權乾淨

Ultralytics YOLO 是 **AGPL-3.0**。學生專題公開在 GitHub 上，AGPL 會傳染到整個
VolleyForm。MediaPipe 是 Apache 2.0，沒有這個問題。

## 6. 誠實的另一面：MediaPipe 輸在哪

不寫這段的比較表沒有可信度。

| 弱點 | 證據 | 現況 |
|---|---|---|
| 精度低於 RTMPose 約 10 個 AP | 引用 | 未在本專案量化 |
| 單人，無法選定目標球員 | 專案已有「追錯人」事故 | **未修** |
| 手部偵測會把兩手疊在同一隻手 | 兩手間距量到 0.0006、手掌寬 0.07 | 已加 gate 擋掉 |
| 遮擋時仍回傳低信心的點 | 本次實測角度可算率僅 69% | 已加畫面外 gate |
| `set` 動作可用關節點僅 55.9% | 本次實測 | 未處理 |
| 0.10.9 版鎖住 numpy，無法與 torch 生態共存 | 本次安裝實測 | 用獨立 venv 迴避 |

## 7. 結論

**留在 MediaPipe，但補追蹤層。**

換模型解決的是「關節點準不準」（約 10 AP），代價是失去瀏覽器部署、失去 3D、
失去手部與臉部點、可能踩 AGPL。而本專案已知的誤判來源——追錯人、階段切割器挑錯
影格、手部偵測重疊——**沒有一個是換模型能解決的**。

投報率排序：

1. **加人物追蹤**（YOLO + ByteTrack，或任何 re-ID）選定目標球員 — 直接解掉追錯人
2. **修階段切割器** — 專案實測誤判率 LOO 10.1%，門檻可歸因只有 4.9%，其餘來自挑錯影格
3. **量 MediaPipe 的角度誤差**（SportsPose 那條線）— 讓報告能寫出誤差界限
4. 換姿態模型 — 排最後

---

## 8. 待補：尚未實測的部分

本文件的 RTMPose / YOLO-Pose 欄位是引用值。要變成實測，執行：

```bash
python tools/pose_benchmark/backends/run_rtmpose.py --frames <frames_dir> --out results/rtmpose.json
```

需先在獨立環境安裝 `rtmlib onnxruntime`（不可裝進專案環境，會與 mediapipe 0.10.9
的 numpy 版本衝突），首次執行會下載約 50MB 權重。

跑完後本文件第 4 節的「實測」標記才能擴及其他模型，第 6 節「精度低於 RTMPose」
才能從引用升級為本專案影片上的實測結論。
