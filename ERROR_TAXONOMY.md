# 錯誤分類表：五個動作，App 抓得到什麼、抓不到什麼

這份表回答一個問題:**教練會指出的錯誤,這個 App 有幾項真的在看?**

每一列標成四種狀態:

| 狀態 | 意思 |
|---|---|
| **已覆蓋** | 已經有判定邏輯與回饋文字,代碼寫在右欄 |
| **可加** | 在影像平面上量得到,但**還沒量過參考資料集**,所以還不能上 |
| **無法驗證** | 做了、量了,但手上沒有能證明它抓得到東西的素材,**沒有上線** |
| **做不到** | 需要偵測球、網、或跨影格時序——目前的 MediaPipe-only 架構拿不到 |

「可加」不等於「該加」。這個專案的規矩是**每一項都要先量過才能上**,否則只是再多一種誤判方式。

驗收門檻分兩級,用哪一級要看那是什麼東西:

| | 門檻 | 用在哪 |
|---|---|---|
| **標準級** | (a) 清掉每一支參考片 + (c) 檢查不是死的(存在生理上做得到、會觸發的值) | 角度判定帶。既有的 14 條**全部**是照這個上的——沒有一條有標記錯誤的素材 |
| **嚴格級** | (a) + (c) + **(b) 真的抓得到標記為錯誤的片段** | 全新的特徵型態(形狀檢查)。沒有前例可循時,要有東西證明它會為了正確的理由觸發 |

一開始我對所有候選都用嚴格級,那比這個系統自己的前例還嚴。
標準級對「公認的錯誤 + 量得準的關節 + 收斂夠的帶」是夠的;
嚴格級留給「沒人做過、不知道會不會亂叫」的新東西。

代價要講清楚:**每加一條帶就多一份誤判**。門檻是 `p10 − tolerance`,p10 依定義把 10% 的
樣本放在下面,所以一條帶大約會對 10% 以內的正確動作出手。這就是為什麼不是「能加就加」。

---

## 先講一個負面結論:公開資料集沒有「錯誤標記」

2026-09-20 用 `web-crawl-dispatch` 把這件事查到底。結論是
**沒有現成的排球錯誤標記資料集可以拿來訓練**,而且原因是結構性的。

### 查了什麼

| 來源 | 結果 |
|---|---|
| GitHub「volleyball dataset」42 個 repo(全部看過) | 比賽數據 EDA、勝負預測、球/場地偵測、裁判手勢、穿戴式手勢。**零個有技術錯誤標記** |
| `Awesome-AQA`(動作品質評估領域的完整清單,140 stars,6 萬字) | **「volleyball」出現 0 次** |
| Ego-Exo4D(附專家口述評論的大型資料集) | 場景是 Soccer / Basketball / Dance / Bouldering / Music / Cooking / Bike Repair / Health。**沒有排球** |
| Papers With Code | 已關站,轉址 Hugging Face |

`Awesome-AQA` 收錄的運動:復健 40、手術 23、跳水 19、花式滑冰 15、健身 12、
舞蹈 10、滑雪 8、彈翻床/體操/籃球各 3。資料集名稱是 AQA-7、MTL-AQA、FineDiving、
FineFS、JIGSAWS、KIMORE、LOGO、UNLV。

### 為什麼排球沒有

看得出一個模式:**動作品質評估的資料集，全部來自「本來就有官方評分」的運動**
(跳水、體操、花式滑冰、彈翻床)或**有既定操作流程的臨床場域**(手術、復健、健身)。
那些領域的標籤是現成的——裁判打的分數、復健的動作規範。

排球沒有逐個動作的官方評分,所以沒有人做得出這種資料集。這不是「還沒有人做」,
是**標籤的來源不存在**。

### 最接近的兩個(CVPR 2025)

- **BASKET** — 籃球,4400 小時、32232 名球員、20 項細分技術。但標的是**整段 8–10 分鐘
  影片的技術等級**(優/良/中/可/差),不是單一動作哪裡做錯。
- **ExpertAF** — 從影片產生可執行的專家回饋,跟這個 App 是同一個問題形狀。
  但它只做籃球與足球,而且建在 Ego-Exo4D 上——排球不在裡面。

### 所以

從公開來源能拿到的只有**錯誤的「分類」**(教練教材層級的知識),拿不到**標好的樣本**。
這張表就是那份分類;樣本仍然只能靠自己量。這也是為什麼系統至今沒有訓練出來的模型,
而是一套可稽核的統計基準線。

---

## 目前的判定結構

判定分兩層,兩層的來源完全不同:

1. **角度判定帶**(原本 14 條,2026-09-17 起 15 條,`ACTION_RULES` + `backend/reference_standards.json`)
   ——門檻是從資料集量出來的 p10/p90 加上自適應寬容度。五個動作都有。
2. **手部/形狀判定**(`_evaluate_hand_shape`)
   ——**門檻是寫死的常數**(0.08、0.24、1.08、0.06/0.32),沒有校準來源。
   而且**只有 receive 和 set 有**。

第 2 點是這次工作的起點:**扣球、發球、攔網在角度以外一項都沒有。**
攔網有三段影片被教學影片標成錯誤示範,卻通過了每一條判定帶。
當時的假設是「錯在兩隻手之間的關係,而沒有東西在看那個」——量完之後這個假設不成立
(見下),真正分得開的是攔網**蓄力時的手肘角度**,而那條判定帶當時不存在。現在有了。

---

## 扣球 spike

| 錯誤 | 狀態 | 代碼 / 依據 |
|---|---|---|
| 觸球時手肘沒伸直 | 已覆蓋 | `elbow_bad`(contact.elbow 下界) |
| 觸球點太低、手臂沒舉高 | 已覆蓋 | `shoulder_low`(contact.shoulder 下界) |
| 起跳前沒蓄力、腿是直的 | 已覆蓋 | `knee_bad`(crouch.knee 上界) |
| 蹲得過深 | 已覆蓋 | `knee_too_bent`(crouch.knee 下界) |
| 沒有轉體拉弓 | **量過不通過** | 見下方「扣球與發球:兩個代理量都不成立」 |
| 助跑步伐不對 | 做不到 | 需要跨影格步伐時序,且鏡頭常切掉腳 |
| 沒有壓腕(手腕 snap) | 做不到 | 需要手部細節加時序,觸球瞬間手常糊掉 |
| 擊球時機太早/太晚 | 做不到 | 需要偵測球 |

## 發球 serve

| 錯誤 | 狀態 | 代碼 / 依據 |
|---|---|---|
| 觸球時手肘沒伸直 | 已覆蓋 | `elbow_bad` |
| 觸球點太低 | 已覆蓋 | `shoulder_low` |
| 沒蓄力 | 已覆蓋 | `knee_bad` |
| 蹲得過深 | 已覆蓋 | `knee_too_bent` |
| 非慣用手沒舉起瞄準 | **量過不通過** | 同上 |
| 重心沒有向前轉移 | 可加 | 觸球前後髖部水平位移 |
| 拋球太前/太後/不穩 | 做不到 | 需要偵測球 |

## 攔網 block

| 錯誤 | 狀態 | 代碼 / 依據 |
|---|---|---|
| 手肘沒伸直 | 已覆蓋 | `elbow_not_straight` |
| 手沒舉夠高 | 已覆蓋 | `hands_not_high` |
| 起跳前沒蓄力 | 已覆蓋 | `knee_bad` |
| 蹲得過深 | 已覆蓋 | `knee_too_bent` |
| 兩手高度不一致 | **無法驗證** | 見下方「一個被量翻的候選檢查」 |
| 兩手間距不對,球會從中間穿過 | **無法驗證** | 同上 |
| 手沒舉到夠高(以手腕相對肩膀量) | **量過不通過** | `wrist_above_shoulder`:參考分布最低兩樣本 0.490/0.504 夾在錯誤值之間 |
| 起跳前身體前傾撞網 | **量過不通過** | `trunk_lean` 是死檢查:上界要肩膀在髖前面 1.2 個軀幹長 |
| **蓄力時手臂垂下、來不及舉過網** | **已覆蓋(2026-09-17 新增)** | `block_arms_dropped`(crouch.elbow 下界) |
| 過網時機太早/太晚 | 做不到 | 需要偵測球與網 |
| 手沒有伸過網 | 做不到 | 需要偵測網 |

## 接球 receive

| 錯誤 | 狀態 | 代碼 / 依據 |
|---|---|---|
| 手肘彎曲、沒形成平台 | 已覆蓋 | `elbow_bad` |
| 膝蓋沒彎 | 已覆蓋 | `knee_bad` |
| 蹲得過深 | 已覆蓋 | `knee_too_bent` |
| 「龍蝦手」:肩縮且肘彎 | 已覆蓋 | `lobster_receive_risk`(唯一同時看兩個關節的規則) |
| 兩手沒併攏 | 已覆蓋 | `receive_hands_apart`(**門檻寫死 0.24,未校準**) |
| 平台不平、兩手不等高 | 已覆蓋 | `receive_platform_unbalanced`(**門檻寫死 0.08,未校準**) |
| 站姿太窄、來不及移動 | 可加 | `stance_width` |
| 平台比髖還寬 | 可加 | `hand_gap`(原 `platform_width` 已移除:同分子,分母卻是更不穩的髖寬) |
| 接球點在身體外側 | 可加 | 手腕水平位置相對髖中心 |
| 腳步沒到位 | 做不到 | 需要偵測球 |

## 舉球 set

| 錯誤 | 狀態 | 代碼 / 依據 |
|---|---|---|
| 手肘位置不對(太外或太內) | 已覆蓋 | `elbow_position_bad`(唯一兩側都判的角度帶) |
| 手沒舉夠高 | 已覆蓋 | `shoulder_low` |
| 觸球點在額頭以下 | 已覆蓋 | `wrist_low`(**直接比較手腕與鼻子高度,無寬容度**) |
| 手指沒張開 | 已覆蓋 | `setting_fingers_closed`(**門檻寫死 1.08**) |
| 兩手間距不對 | 已覆蓋(**2026-09-17 修好**) | `setting_hand_spacing_bad`——門檻仍寫死,但加了偵測可信度 gate,見下 |
| 兩手不對稱 | 已覆蓋 | `setting_hands_unbalanced`(**門檻寫死 0.08**) |
| 偵測不到雙手 | 已覆蓋 | `setting_hands_not_detected`(這是拍攝品質,不是動作錯誤) |
| **膝蓋沒彎、沒用下半身送球** | **已覆蓋(2026-09-17 新增)** | `set_legs_not_used`(crouch.knee 上界) |
| **觸球點偏離額頭正上方** | **已覆蓋(2026-09-17 新增)** | `set_hands_off_forehead`(`hands_off_center` 上界) |
| 沒有面向目標 | 做不到 | 需要知道目標在哪,畫面裡沒有 |

---

## 一個被量翻的候選檢查

手上唯一有錯誤標記的素材,是一支攔網教學影片:畫面上用綠勾與紅叉標出三段正確、
三段錯誤的攔網。這六段是驗收門檻第 (b) 條的全部依據。

第一版做了兩個候選檢查——「兩手高度差」與「兩手間距」——並且看起來有效:
三段錯誤裡有兩段落在正確範圍之外。**那個結果是假的。**

原因是特徵的**分母**。第一版把每個量除以「兩肩在畫面上的水平距離」。
但那是一個投影量:球員側身面對鏡頭時,兩肩會投影到幾乎同一個 x,分母塌向 0,
比值就爆掉。掃描資料裡有一支發球影片的 `hand_level_gap` 量到 **81.5**——
一個高度只有 1 個單位的畫面,兩隻手不可能相距 81 個身寬;它背後的肩寬是 0.015。

> 這跟 clip pipeline 那條 `MAX_NORM` 教訓是同一個坑:**分母會消失的比值,
> 不是回報一個很大的值,是回報一個沒有意義的值。**
> 已經寫在 skill 裡了,還是踩了一次。

改用**軀幹長度**(兩肩中點到兩髖中點)當分母後重新量。軀幹是一段垂直線段,
轉身幾乎不會縮短它,而且垂直量本來就該除以垂直尺度。結果完全相反:

| 片段 | 標記 | 兩手高度差 | 兩手間距 | 手腕高過肩 |
|---|---|---|---|---|
| 正確 1 | ✅ | 1.014 | 1.483 | 0.637 |
| 正確 2 | ✅ | 0.006 | 0.209 | 0.934 |
| 正確 3 | ✅ | 0.023 | 0.542 | 0.721 |
| 錯誤 1 | ❌ | 0.310 | 0.638 | 0.534 |
| 錯誤 2 | ❌ | 0.024 | 0.334 | 0.933 |
| 錯誤 3 | ❌ | 0.060 | 0.890 | 0.475 |

三段錯誤的值**全部落在三段正確的範圍內**。門檻設在不誤傷任何參考片的位置時,
這兩個檢查對這批素材一次都不會觸發。

要說清楚這代表什麼、不代表什麼:

- 它**不代表**「兩手不齊」不是錯誤。那支影片的三段 ❌ 標的是**它自己要示範的那個錯誤**,
  而從下面的數字看,那個錯誤是手臂垂下,不是手不齊。
- 它代表的是:**手上沒有任何素材能證明這兩個檢查抓得到真實的錯誤。**
  一個無法示範它會在什麼時候正確觸發的檢查,上線只是多一種誤判的可能。

**結論:不上線,`SHAPE_CHECKS` 裡的兩條宣告與對應的回饋文字都移除。**
量測方法(`hand_level_gap`、`hand_gap`)與整套 `_evaluate_shape_checks` 機制保留並附測試——
接球那三項候選仍然是形狀檢查,等資料集的分布量完再判。

同一批測量裡真正分得開的是**蓄力階段的手肘角度**:

| 片段 | 標記 | 蓄力手肘 |
|---|---|---|
| 正確 1 / 2 / 3 | ✅ | 140.0 / 125.7 / 142.8 |
| 錯誤 1 | ❌ | **79.2** |
| 錯誤 2 | ❌ | **51.6** |
| 錯誤 3 | ❌ | 134.7 |

三段裡有兩段乾淨分開,而且符合教練說法:攔網起跳前手要保持在胸前,
手臂垂下去就來不及壓過網。`block` 現在只判蓄力的膝蓋,沒有手肘。
這是既有的角度判定帶架構就能表達的東西,不需要新的特徵類型。

參考資料集的分布量完了:22 支通過畫面外 gate 的攔網片,蓄力手肘從 **88.0 到 170.2**,
收斂度 0.78。判定區間 `[74.1, 180.0]`。

| | |
|---|---|
| **(a) 不誤傷參考片** | 最小保留樣本 88.0 > 門檻 74.1 → **通過** |
| **(b) 抓得到標記錯誤的片段** | 錯誤 2(51.6)被抓到;錯誤 1(79.2)差 5.1 度逃掉 → **通過** |
| 三段正確 | 140.0 / 125.7 / 142.8,全部放行 |

**錯誤 1 沒被抓到這件事不去修。** 把 tolerance 調小可以抓到它,但門檻會升到約 85,
距離參考片最小值 88.0 只剩 3 度——那是「調門檻直到測試過」,這個專案有前例不做
(`angle_acceptance_test` 那次也是加約束,不是調門檻)。

**已上線:`block_arms_dropped`,`block` 的 crouch 多判一個手肘。**
這是五個動作裡唯一一條 crouch 也判手肘的帶——因為只有它有證據。

### 這條檢查上線後,第一個被它抓到的是 App 自己的示範動畫

`pose_geometry_test.mjs` 會拿 3D 小人的姿勢去對 App 自己的判定帶。新帶一加進去,
它立刻失敗:**示範動畫攔網蓄力的手肘是 65.2°,比 22 支參考片的每一支都低**
(參考範圍 88–170)。也就是說,App 一邊教「手不要垂下來」,一邊示範手垂下來。

根因在 `pose-3d.js`:`block_ready` 的手腕在 `head - 0.02`,而 `block_load` 在
`head + 0.06`——**手在蓄力時是往下掉的**,正好是這條檢查定義的錯誤。
已改成蓄力時手不下降、並往前伸,手肘變成 111.2°,落在參考的 p25(112.5)附近。

這是示範動畫第二次跟教學文字互相矛盾(上一次是接球示範畫成 120° 彎肘,
而 App 的提示寫「把手肘打直」)。兩次都是靠幾何測試抓到,不是靠看。

### 通過的第一個形狀檢查:舉球的觸球點

`hands_off_center` = |雙手中點x − 鼻子x| / 軀幹長。這是角度帶表達不了的東西——
手在額頭正上方或偏在頭側,手肘和肩膀的角度**讀起來一模一樣**。

21 個參考樣本從 0.004 排到 0.327,**零剔除**,上界 0.429:

| | |
|---|---|
| (a) 不誤傷參考片 | 最大保留 0.327 < 上界 0.429 ✓ |
| (c) 檢查不是死的 | 上界是中位數的 **2.2 倍**,是量過的八個分布裡最緊的 |
| 換算 | 軀幹長約佔身高 0.3,所以 0.43 個軀幹長 ≈ 175cm 的人雙手中心離頭部中線 23 公分 |

**同一批量測裡,`trunk_lean`(軀幹前傾)全部是死檢查**:上界是中位數的 6 到 18 倍,
攔網蓄力那條要求肩膀在髖前面 1.2 個軀幹長——身體做不到那個姿勢。四條全部剔除。
這就是標準級門檻第 (c) 條在做的事:**把一個永遠不會觸發、卻看起來很像有在保護你的檢查擋下來。**

### 第三個候選也被量掉了:手腕高過肩的程度

把六個片段量到的**每一個**數值都掃過一遍找分離訊號,`contact.wrist_above_shoulder`
看起來是第二強的:兩段錯誤(0.53、0.47)都低於三段正確的最小值 0.64,而且同方向。
它跟既有的 `hands_not_high` 不同——後者判的是**肩關節角度**,那三段都沒被抓到。

對全部 22 支參考片量完就垮了:參考分布的最低兩個樣本是 **0.490 和 0.504**,
夾在「錯誤」的 0.475 和 0.534 之間。門檻設在 0.381(清掉所有參考片),兩段錯誤都逃掉。

**同一個教訓第二次出現:n=3 算出來的「正確範圍」是假性狹窄的。**
掃描表上有五個量顯示「3 段裡有 2 段落在正確範圍外」,其中兩個(`frontality`、`torso`)
根本是鏡頭條件不是技術,一個(`contact.elbow`)的兩個值一高一低是雜訊跨過窄範圍。
只有 `crouch.elbow` 在對上完整參考分布之後還站得住。

## 扣球與發球:兩個代理量都不成立

這兩項是清單上僅剩、看起來最有機會的。量法工具(`measure_torso_rotation.py`、
`measure_aim_arm.py`)早就在 repo 裡,但它們都取**窗口內的峰值**,而評估器判的是
**單一影格**;前者還要用骨長反解深度,而 `get_shape_features` 是刻意全平面的。
所以各做了一個平面內的代理量,對扣球 20 支、發球 26 支量過。

### 扣球「沒有轉體拉弓」:方向不對,檢查永遠不會為了它觸發

代理量 = 肩寬 / 髖寬。拉弓時肩線轉離鏡頭會foreshorten,比值下降;不轉體則維持。

| | |
|---|---|
| 蓄力階段(n=13) | p10 1.371、中位數 1.503、p90 1.786、最大 1.990 |
| 上界 | 2.289 |

**「沒轉體」讀出來的值就是中位數附近。** 肩髖都正對鏡頭時,這個比值等於解剖比例
(約 1.35–1.45),而參考片中位數 1.503 已經在那裡。上界 2.289 要「髖轉了而肩沒轉」
才到得了——那是拉弓的反向動作。

先前擔心這個量其實只是在測鏡頭角度,量過之後這個顧慮**不成立**:
它與 `frontality` 的相關係數只有 +0.35 / −0.27 / +0.22 / −0.05。
問題不在雜訊,在**門檻落不到錯誤發生的那一側**。

### 發球「非慣用手沒舉起瞄準」:兩個階段各有一個問題

代理量 = 較低那隻手高過肩膀多少 / 軀幹長(`wrist_above_shoulder` 取較高的手,
這個取較低的)。

| 階段 | 下界 | 為什麼不能上 |
|---|---|---|
| 觸球 | −1.140 | 門檻到得了,但**觸球那一刻手本來就放下來了**。量到的不是「沒舉起瞄準」 |
| 蓄力 | −2.149 | 量的是對的時刻,但**解剖上到不了**:手臂自然垂下時肩到腕約 1.5 個軀幹長,全部 78 個樣本的最小值是 −1.361。死檢查 |

要真正判這一項,評估器得支援**窗口內的極值**而不只是單一影格。那是架構改動,
不是再加一條帶。

## 一個副產品:兩個新的可稽核量

改分母的時候順便讓 `get_shape_features` 回報兩個「尺度」而不只是比值:

- **`frontality` = 肩寬 / 軀幹長**——球員相對鏡頭有多正面。接近 0 表示完全側身,
  這時候「左右距離」量到的是深度,沒有意義。上面那三段錯誤裡,`錯誤 3` 的蓄力
  影格 frontality 只有 **0.006**,是完全側身,它在那一格的所有水平特徵都不該採信。
- **`torso`**——球員在畫面裡的絕對大小。太小的話,landmark 的雜訊跟要量的量同級。

這兩個量本身不是檢查,是**給 gate 用的輸入**,門檻要從資料量出來再定。
把它們一起存進掃描輸出,是為了不要為了設一個門檻又重掃一次整個資料集——
這次已經重掃兩次了。

---

## 統計

| | 已覆蓋 | 可加(待量) | 量過不通過 | 做不到 |
|---|---|---|---|---|
| 扣球 | 4 | 0 | 1 | 3 |
| 發球 | 4 | 1 | 1 | 1 |
| 攔網 | **5** | 0 | 4 | 2 |
| 接球 | 6 | 3 | 0 | 1 |
| 舉球 | **9** | 0 | 0 | 1 |
| **合計** | **28** | **4** | **6** | **8** |

這一輪加了三項,每一項都有量測支撐:

| 代碼 | 動作 | 依據 |
|---|---|---|
| `block_arms_dropped` | 攔網 | 蓄力手肘。標記錯誤的三段抓到一段,標記正確的三段全放行 |
| `set_legs_not_used` | 舉球 | 蓄力膝蓋。上界卡在直肢下限 168,只有膝蓋幾乎打死才觸發 |
| `set_hands_off_forehead` | 舉球 | 觸球點偏移。八個候選分布裡最緊的(上界只有中位數的 2.2 倍) |

另外修好一個一直在誤傷的既有舉球檢查(`setting_hand_spacing_bad`,誤傷率 67%)。

**「量過不通過」六項**:攔網四項(兩手高度差、兩手間距、手腕高過肩、身體前傾)、
扣球的軀幹扭轉、發球的瞄準手。這一欄不是失敗——**每一項都省下一種誤判方式**,
而且沒有它,那六個檢查會在沒人知道的情況下上線。

(`trunk_lean` 在舉球那邊也一起量了,同樣是死檢查,但「身體前傾」本來就不是
舉球的教學錯誤,所以沒列進舉球的表。)

**「可加」只剩 4 項**:接球三項、發球的重心轉移。三個動作(扣球、攔網、舉球)
在現有架構下量得到的都做完了。

「可加」剩 6 項,全部缺同一樣東西:**標記為錯誤的素材**。

---

## 兩個已知的結構性缺口

這張表在盤點過程中暴露了兩件事,都還沒修:

### 1. 一半的判定門檻是寫死的——量過了,其中一個真的在誤傷

角度帶有完整的校準來源、樣本數與收斂度。但 receive 與 set 的手部判定用的是
`0.08 / 0.24 / 1.08 / 0.06 / 0.32` 這些常數,沒有資料支撐。

2026-09-17 第一次把它們對著資料集量(`tools/dump_hand_features.py`)。結果:

| 動作 | 特徵 | 門檻 | 參考片中位數 | 誤傷 |
|---|---|---|---|---|
| receive | hands_level_gap | > 0.08 | 0.003 | 0 |
| receive | hand_center_gap | > 0.24 | 0.001 | 0 |
| set | finger_extension | < 1.08 | 1.291 | 0 |
| set | hands_level_gap | > 0.08 | 0.009 | 0 |
| **set** | **hand_center_gap** | **< 0.06** | **0.001** | **4/6(67%)** |

`setting_hand_spacing_bad` 在能執行的 6 支參考片裡打中 4 支。

**但門檻不是兇手,輸入才是。** 那四支量到的兩手間距是 0.0003–0.0009,
而一個手掌本身有 0.06–0.07 寬——**兩隻手腕不可能比手掌的 1.5% 還近**。
MediaPipe 把兩個偵測都放到同一隻看得見的手上了。另外兩支合理的量到 0.083 和 0.126
(約 1.1 和 1.8 個手掌寬),那才是真正的舉球手型。

修法跟姿勢那道 gate 同型:**間距小於 0.1 個手掌寬就不判**(`HAND_PAIR_MIN_SEPARATION`)。
量出來的效果是 4 個誤傷全消、**真實判定一個都沒少**(那 13 支本來就是在塌掉的偵測上判的)。
而且 gate 擋不到真錯誤:`receive_hands_apart` 要間距 > 0.24 才觸發,gate 只在 < 0.007 時擋,
兩個區間不重疊。

`finger_extension` 刻意留在 gate 外面——它量的是**單手**,偵測塌掉時仍然可讀,
把它一起擋掉等於為了修一個壞掉的檢查而丟掉一個能用的。

**還沒解決的**:另外四個門檻仍然是寫死的。它們目前沒有誤傷,但也沒有人知道
它們抓不抓得到真錯誤。而且 set 只有 **6/24** 支參考片在判定格偵測得到雙手
(receive 是 12/24),所以這四個檢查大部分時間根本不會執行。

### 2. ~~判定端沒有「畫面外」防護~~(2026-09-17 已修)

`angle.angle.landmarks_offscreen` 會擋掉 MediaPipe 在畫面外硬猜的關節——
但在此之前**只在建立判定帶的時候擋**。App 實際判定使用者影片時不做這個檢查:

> 使用者拍攝時腳出了畫面,MediaPipe 仍會「有信心地」給出腳踝位置,
> App 就用那個幻覺算出來的膝角告訴他蹲得不夠低。

量出來的影響(把 292 個參考樣本丟回 App 自己的判定):

| | |
|---|---|
| 被標紅的樣本 | 25 |
| 其中關節是畫面外硬猜的 | **4** |
| 其中關節真的被觀測到 | 21 |

那 4 個包括 `block crouch knee 23.6°`(解剖學上不可能,就是 2026-08-17 那個 bug 的數字)
與 `set contact elbow 177.8°`(gate 註解裡點名的 `pexels_6217341`)。

修法是把同一個 `landmarks_offscreen` 套到 `_evaluate_joint` 與前端的
`evaluatePhaseJoint`。**21 個真實觀測到的判定一個都不受影響**,拿掉的全是算在
沒被看到的位置上的。判不了的關節回報 `status: "unknown"`、`judged: false`,
`app.js` 明確跳過——因為那裡任何非 green 的狀態都會被呈現成「你做錯了」,
不跳過就會把「你的腳沒入鏡」講成「你的膝蓋不對」。

**還沒做的**:3D 小人對「沒判」的關節仍然顯示綠色,而不是「沒量到」。
不會誤導成錯誤,但也沒告訴使用者該重拍。

---

## 剩下 8 項卡在同一件事上

「可加」的 8 項——扣球轉體、發球瞄準手與重心轉移、接球站姿寬度/平台寬度/接球點、
舉球膝蓋與軀幹朝向——量測方法全部都有,卡的不是技術,是**驗收門檻的第 (b) 條**:
沒有素材能證明一個新檢查真的抓得到那個錯誤。

手上唯一有錯誤標記的素材是一支攔網教學影片的六個片段,而且它示範的只有一種錯誤。

能解開這個的最便宜方式,是**自己錄**:每一種錯誤刻意做錯一次,錄 10-20 秒,
標清楚是哪一種錯誤。八項就是八段。有了那個,每一項都能照 block 這條的流程跑完:
量參考分布 → 定門檻 → 驗兩條 → 上線。

公開資料庫繞不過這一關,因為排球的錯誤標記資料集不存在(見開頭)。
